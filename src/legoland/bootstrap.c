#include "legoland.h"

/* Dummy PE entry point so the image links. Not a real game function; the real
   entry point lives in the CRT translation unit and takes over once decompiled. */
void legoland_entry(void) { STUB(); }
