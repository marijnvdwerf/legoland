# Build output
build/

# Downloaded toolchain (populated by `uv run setup.py`) — never committed
toolchain/

# Python / uv
.venv/
__pycache__/
*.pyc

# reccmp machine-local config (paths/build artifacts)
reccmp-user.yml
reccmp-build.yml

# clangd config (absolute toolchain path; regenerated by setup.py)
.clangd

# loop/agent local state
.claude/

# stray cvdump temp files written into cwd
/a[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]??
report.json
