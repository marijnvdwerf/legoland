# Build output
build/

# Downloaded toolchain (populated by `uv run setup.py`) — never committed
toolchain/

# Python / uv
.venv/
__pycache__/
*.pyc

# reccmp machine-local config (paths/build artifacts)
reccmp-user.yml
reccmp-build.yml
